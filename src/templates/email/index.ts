import { generate_password } from "./generate_password"
import { novaContaAdm } from "./novaContaAdm"
import { novaContaUsuario } from "./novaContaUsuario"

export default { generate_password, novaContaAdm, novaContaUsuario }
